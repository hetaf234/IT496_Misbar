# 03 — Dataset integrity

Checks image decoding, counts, dimensions, corrupted files, and reconciliation of the actual images with `final_dataset_manifest_v2.csv`. This stage requires the actual `final_food_dataset_v2` images.
